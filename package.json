{
  "name": "near",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ near",
    "create-local": "graph create --node http://localhost:8020/ near",
    "remove-local": "graph remove --node http://localhost:8020/ near",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 near",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.0",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
